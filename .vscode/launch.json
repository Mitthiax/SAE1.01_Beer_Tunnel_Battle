{
    "configurations": [
        {
            "type": "by-gdb",
            "request": "launch",
            "name": "Launch(gdb) BeerTunnelsBattles",
            "program": "BeerTunnelsBattles",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "debug BeerTunnelsBattles"
        }
    ]
}